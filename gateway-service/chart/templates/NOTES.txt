1. Pod:
   kubectl get pods -n {{ .Release.Namespace }} -l app.kubernetes.io/instance={{ .Release.Name }}

2. Service:
   kubectl get svc -n {{ .Release.Namespace }} {{ include "algorycode-gateway.fullname" . }}

3. Downstream URI değerleri values içindeki gateway.routes[].uri ile gelir; Eureka kullanılmaz.

4. Rent ile aynı namespace’te örnek URI:
   http://REL-algorycode-rent-service.{{ .Release.Namespace }}.svc.cluster.local:8090

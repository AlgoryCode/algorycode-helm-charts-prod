1. Pod:
   kubectl get pods -n {{ .Release.Namespace }} -l app.kubernetes.io/instance={{ .Release.Name }}

2. Service ({{ .Values.service.type }}):
   kubectl get svc -n {{ .Release.Namespace }} {{ include "algorycode-rent-auth-service.fullname" . }}

3. Yerel test (ClusterIP):
   kubectl -n {{ .Release.Namespace }} port-forward svc/{{ include "algorycode-rent-auth-service.fullname" . }} {{ .Values.service.port }}:{{ .Values.service.port }}
   curl -s http://127.0.0.1:{{ .Values.service.port }}/actuator/health

4. Üretim sırları: secret değerlerini repoya yazmayın; Sealed Secrets / External Secrets / CI secret kullanın.

5. Küme içi DNS: http://{{ include "algorycode-rent-auth-service.fullname" . }}.{{ .Release.Namespace }}.svc.cluster.local:{{ .Values.service.port }}
